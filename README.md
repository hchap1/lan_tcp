# lan_tcp
Starts a TPC server and broadcasts existence over UDP. If, prior to establishing the server, the UDP broadcast of an existing server is received then instead create a client connecting to that server.
